import { Routes } from '@angular/router';
import { HackingWindowComponent } from './hacking-window/hacking-window.component';

export const routes: Routes = [
    {
        path: "",
        component: HackingWindowComponent
    }
];
